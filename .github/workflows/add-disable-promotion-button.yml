name: Add disable promotion button

on:
  push:
    branches: [work-deliverylv-adm]
    paths:
      - '.github/scripts/add-disable-promotion-button.py'
      - '.github/workflows/add-disable-promotion-button.yml'

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: work-deliverylv-adm
          fetch-depth: 0
      - name: Preserve patch script
        run: cp .github/scripts/add-disable-promotion-button.py /tmp/add-disable-promotion-button.py
      - name: Patch main
        shell: bash
        run: |
          set -euo pipefail
          git checkout main
          git pull --ff-only origin main
          python /tmp/add-disable-promotion-button.py
          python - <<'PY'
          import re, pathlib
          text=pathlib.Path('index.html').read_text(encoding='utf-8')
          scripts='\n'.join(re.findall(r'<script(?:\s[^>]*)?>(.*?)</script>',text,re.S))
          pathlib.Path('/tmp/admin-inline.js').write_text(scripts,encoding='utf-8')
          assert 'data-disable-promotion' in text
          assert 'disablePromotion' in text
          PY
          node --check /tmp/admin-inline.js
          git config user.name 'jhonatabrasileia666-spec'
          git config user.email 'jhonata.brasileia666@gmail.com'
          git add index.html
          git diff --cached --check
          git commit -m 'feat: add one-tap promotion disable' || exit 0
          git push origin main
