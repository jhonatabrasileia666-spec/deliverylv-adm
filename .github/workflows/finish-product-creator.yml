name: Finish product creator
on:
  push:
    branches: [main]
    paths:
      - '.github/scripts/finish_product_creator.py'
      - '.github/workflows/finish-product-creator.yml'
permissions:
  contents: write
jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.x'
      - run: python .github/scripts/finish_product_creator.py
      - name: Commit result and cleanup
        run: |
          rm -f .github/scripts/finish_product_creator.py .github/workflows/finish-product-creator.yml
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add index.html .github
          git commit -m 'feat: finish flexible product creation' || exit 0
          git push
