name: Apply switch access button
on:
  push:
    branches: [main]
    paths:
      - '.github/scripts/add_switch_access.py'
      - '.github/workflows/apply-switch-access.yml'
permissions:
  contents: write
jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Patch switch access
        run: python .github/scripts/add_switch_access.py
      - name: Validate switch access
        run: |
          grep -q "switch-login-button" index.html
          grep -q "Trocar acesso" index.html
          grep -q "Escolha como deseja entrar" index.html
      - name: Commit final change
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          rm -f .github/scripts/add_switch_access.py .github/workflows/apply-switch-access.yml
          git add -A
          git commit -m "feat: add switch access button"
          git push
