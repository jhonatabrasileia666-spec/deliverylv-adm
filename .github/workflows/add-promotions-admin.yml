name: Add admin promotions

on:
  push:
    branches: [work-deliverylv-adm]
    paths:
      - '.github/scripts/add-promotions-admin.py'
      - '.github/workflows/add-promotions-admin.yml'

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: work-deliverylv-adm
          fetch-depth: 0
      - name: Preserve patch script
        run: cp .github/scripts/add-promotions-admin.py /tmp/add-promotions-admin.py
      - name: Patch main
        shell: bash
        run: |
          set -euo pipefail
          git checkout main
          git pull --ff-only origin main
          python /tmp/add-promotions-admin.py
          python - <<'PY'
          import re, pathlib
          text=pathlib.Path('index.html').read_text(encoding='utf-8')
          scripts='\n'.join(re.findall(r'<script(?:\s[^>]*)?>(.*?)</script>',text,re.S))
          pathlib.Path('/tmp/admin-inline.js').write_text(scripts,encoding='utf-8')
          assert 'promotion-manager' in text
          assert 'savePromotions' in text
          assert 'promotion_active' in text
          PY
          node --check /tmp/admin-inline.js
          git config user.name 'jhonatabrasileia666-spec'
          git config user.email 'jhonata.brasileia666@gmail.com'
          git add index.html
          git diff --cached --check
          git commit -m 'feat: add promotion manager' || exit 0
          git push origin main
