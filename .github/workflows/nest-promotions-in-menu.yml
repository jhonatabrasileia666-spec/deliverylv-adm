name: Nest promotions in menu

on:
  push:
    branches: [work-deliverylv-adm]
    paths:
      - '.github/scripts/nest-promotions-in-menu.py'
      - '.github/workflows/nest-promotions-in-menu.yml'

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: work-deliverylv-adm
          fetch-depth: 0
      - name: Preserve patch script
        run: cp .github/scripts/nest-promotions-in-menu.py /tmp/nest-promotions-in-menu.py
      - name: Patch main
        shell: bash
        run: |
          set -euo pipefail
          git checkout main
          git pull --ff-only origin main
          python /tmp/nest-promotions-in-menu.py
          python - <<'PY'
          import re, pathlib
          text=pathlib.Path('index.html').read_text(encoding='utf-8')
          scripts='\n'.join(re.findall(r'<script(?:\s[^>]*)?>(.*?)</script>',text,re.S))
          pathlib.Path('/tmp/admin-inline.js').write_text(scripts,encoding='utf-8')
          assert 'data-menu-subtab="products"' in text
          assert 'data-menu-subtab="promotions"' in text
          assert 'data-menu-subpanel="products"' in text
          assert 'data-menu-subpanel="promotions"' in text
          assert 'data-view="promotions"' not in text
          assert 'id="view-promotions"' not in text
          assert text.count('id="promotion-manager"') == 1
          PY
          node --check /tmp/admin-inline.js
          git config user.name 'jhonatabrasileia666-spec'
          git config user.email 'jhonata.brasileia666@gmail.com'
          git add index.html
          git diff --cached --check
          git commit -m 'feat: move promotions inside menu tabs' || exit 0
          git push origin main
